1. for 문은 항상 인덱스로 셀 것(활용도 측면에서 더 좋다)

2. 반복문에서 pop등으로 순회하는 리스트의 길이가 바뀐다면, for 대신 while 사용.

    그리고 현재 인덱스를 검사하는데, 현재 인덱스를 제거한다면
    다음 검사에서는 인덱스를 그대로 가져가야됨. (한 칸씩 앞으로 땡겨지기때문에)
    원래는 i += 1등을 함수 맨 끝에 써서 for과 비슷한 효과를 내지만,
    pop으로 빼면 continue등으로 i += 1을 넘겨야 한다.



